# Compatibility Notes

- Debugging assumes canonical setup from onboarding is complete.
- Browser autoplay constraints can mimic integration failures.
- Subscriber mode requires explicit cleanup for dynamically managed listeners.
