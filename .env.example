# Zusound currently has no required environment variables.
# Keep this file as the single source-of-truth for env configuration.
# If a variable is introduced, document it in docs/CONTRIB.md.
GITHUB_TOKEN=GITHUB_TOKEN